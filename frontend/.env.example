# Configuración del Backend API
# ==============================
# 
# ⚠️ IMPORTANTE: Este archivo es para DESARROLLO LOCAL
# En PRODUCCIÓN (Rocky Linux), el frontend detecta automáticamente
# que debe usar /api en lugar de localhost

# URL del backend API (SOLO para desarrollo local)
# Descomenta esta línea si quieres forzar una URL específica:
# VITE_API_URL=http://localhost:3000

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📝 NOTA: Detección automática
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# El frontend ahora detecta automáticamente el entorno:
#
# ✅ Desarrollo (npm run dev):
#    → Usa http://localhost:3000
#
# ✅ Producción (npm run build):
#    → Detecta el dominio actual
#    → Usa {origin}/api automáticamente
#
# Ejemplos en producción:
#   http://lumo.anima.edu.uy → http://lumo.anima.edu.uy/api
#   https://lumo.anima.edu.uy → https://lumo.anima.edu.uy/api
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Otras configuraciones (opcional)
# VITE_API_TIMEOUT=5000
# VITE_ENABLE_DEV_TOOLS=false
