# 🔐 Variables de entorno del Backend LUMO - PRODUCCIÓN
# ===========================================================
# Archivo: backend/.env.production
# 
# ⚠️ IMPORTANTE: Este es un archivo de ejemplo
# Copiar a .env y configurar con valores reales de producción
# 
# Comando: cp backend/.env.production backend/.env
# Luego editar: nano backend/.env
# ===========================================================

# 🗄️ Base de datos
# Para SQLite (desarrollo/testing):
DATABASE_URL="file:./prisma/dev.db"

# Para PostgreSQL (producción recomendada):
# DATABASE_URL="postgresql://lumo_user:STRONG_PASSWORD_HERE@localhost:5432/lumo_db?schema=public"

# 🔐 JWT Configuration
# ⚠️ CAMBIAR ESTE SECRET EN PRODUCCIÓN - Usar valor aleatorio y seguro
# Generar con: openssl rand -base64 32
JWT_SECRET="REPLACE_WITH_STRONG_RANDOM_SECRET_DO_NOT_USE_DEFAULT"
JWT_EXPIRES_IN="7d"

# 🌐 Configuración del Servidor
PORT=3000
# ⚠️ IMPORTANTE: En producción, usar 127.0.0.1 para que solo Nginx acceda
HOST=127.0.0.1

# 🌐 URL del Frontend (para CORS)
# ⚠️ Usar el dominio público sin trailing slash
FRONTEND_URL=http://lumo.anima.edu.uy

# Si tienes HTTPS configurado:
# FRONTEND_URL=https://lumo.anima.edu.uy

# 🚀 Entorno
NODE_ENV=production

# 📧 Email Configuration (opcional - para notificaciones)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@lumo.anima.edu.uy

# 📦 Upload Configuration (opcional)
# MAX_FILE_SIZE=10485760  # 10MB en bytes
# UPLOAD_PATH=./uploads

# 🔒 Security Headers (opcional)
# CORS_CREDENTIALS=true
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutos
# RATE_LIMIT_MAX_REQUESTS=100

# 📊 Logging (opcional)
# LOG_LEVEL=info  # debug, info, warn, error
# LOG_FILE=./logs/app.log

# ===========================================================
# 📝 NOTAS DE SEGURIDAD:
# ===========================================================
# 1. NUNCA commitear este archivo con valores reales a Git
# 2. Usar permisos restrictivos: chmod 600 backend/.env
# 3. Generar JWT_SECRET único y aleatorio
# 4. Cambiar passwords de base de datos
# 5. Usar HTTPS en producción
# 6. Configurar firewall para bloquear acceso directo al puerto 3000
# ===========================================================
